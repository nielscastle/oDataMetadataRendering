# oDataMetadataRendering

Call as

    <prg> <metadata xml file> <dot output file> <optional center entity>

Render dot output with

    /usr/local/bin/dot  -Tpdf /tmp/ER.dot  -o /tmp/ER.pdf && open /tmp/ER.pdf
